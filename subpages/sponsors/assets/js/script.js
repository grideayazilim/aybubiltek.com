/* =============== SCROLL REVEAL ANIMATION =============== */
sr.reveal(`.title-area`);
sr.reveal(`.big-title`, { delay: 300, interval: 500 });
sr.reveal(`.collaborator`, {
  distance: "20px",
  duration: 2000,
  delay: 400,
  interval: 200,
});