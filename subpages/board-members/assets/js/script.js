/* =============== SCROLL REVEAL ANIMATION =============== */
sr.reveal(`.title-area`);
sr.reveal(`.big-title`, { delay: 500, viewFactor: 1});
sr.reveal(`.chairman-card`, { delay: 500, interval: 500 });
sr.reveal(`.member-card`, { delay: 600, interval: 500 });